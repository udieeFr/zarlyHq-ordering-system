# Django Settings
DEBUG=False
SECRET_KEY=&a#*l0%ld-*em6jddy=0s9lga#y3t4j*of#2^+u=_7wca!8s$$b
DB_NAME=zarlyDB
DB_USER=postgres
DB_PASSWORD=passwordDBzarlyhq1401

# Allowed Hosts
ALLOWED_HOSTS=zarlybigfood.my,www.zarlybigfood.my

# Redis — shared cache for OTP confirmation codes and rate limiting across
# gunicorn workers. Points at the internal `redis` service in docker-compose.
# REQUIRED in production: without it the cache falls back to per-process memory
# and OTP verification fails across workers (orders get wrongly cancelled).
REDIS_URL=redis://redis:6379/0

# Stripe Configuration (test keys — swap to live after Stripe approval)
STRIPE_PUBLISHABLE_KEY=pk_test_REDACTED
STRIPE_SECRET_KEY=sk_test_REDACTED
STRIPE_WEBHOOK_SECRET=whsec_REDACTED

# Support Chat Encryption
FERNET_KEY=OIDyKuknfUWEW1wue0-Z9_HOveRO5KmzvCOXNMto6dk=

# Email Configuration (fill in when ready, app works without it)
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password
